numbers = [1,2,3,4,5]
# map(function, iterable) => map applies function on each element of iterable
doubled = list(map(lambda x: x*2, numbers)) # x = input; x*2 = output
print(doubled) # [2, 4, 6, 8, 10
